@model QuanLyKhoLinhKienPC.Models.VaiTro

@{
    ViewData["Title"] = "Chi Tiết Vai Trò";
}

<div class="container-fluid pt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow rounded-4 border-0" style="box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.05) !important;">
                <div class="card-header bg-white border-bottom border-light py-3">
                    <h4 class="mb-0 text-info fw-bold"><i class="fa-solid fa-eye me-1"></i> Chi Tiết Vai Trò</h4>
                </div>
                <div class="card-body p-4">
                    <dl class="row mb-0">
                        <dt class="col-sm-4 text-muted fw-semibold">Tên vai trò:</dt>
                        <dd class="col-sm-8 fw-bold text-dark fs-5">@Html.DisplayFor(model => model.TenVaiTro)</dd>

                        <dt class="col-sm-4 text-muted">Trạng thái:</dt>
                        <dd class="col-sm-8">
                            @if (Model.IsDeleted)
                            {
                                <span class="badge rounded-pill bg-danger bg-opacity-10 text-danger px-3">Đã xóa</span>
                            }
                            else
                            {
                                <span class="badge rounded-pill bg-success bg-opacity-10 text-success px-3">Đang hoạt động</span>
                            }
                        </dd>
                    </dl>
                </div>
                <div class="card-footer bg-white border-top-0 p-4 pt-0 d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-light border shadow-sm rounded-pill px-4 text-secondary fw-semibold">
                        <i class="fa-solid fa-arrow-left me-1"></i> Quay lại
                    </a>
                    <a asp-action="Edit" asp-route-id="@Model?.MaVaiTro" class="btn btn-warning shadow-sm rounded-pill px-4 fw-bold text-dark">
                        <i class="fa-solid fa-pen me-1"></i> Chỉnh sửa
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>